{
  "name": "client-side-config-provider",
  "version": "1.0.0",
  "description": "Provides config (UA, Proxy) for a client-side 2-slot traffic generator.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@google/genai": "latest",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
